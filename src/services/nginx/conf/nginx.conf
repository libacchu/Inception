
server {
	listen 80;
	# listen 443 ssl;
	# listen [::]:443 ssl;
	
	server_name libacchu.42.fr www.libacchu.42.fr;

	ssl_certificate 	/etc/ssl/libacchu.42.fr.crt;
	ssl_certificate_key /etc/ssl/libacchu.42.fr.key;

	ssl_protocols TLSv1.2 TLSv1.3;
	
	root /var/www/html/;
	index index.php index.html;
	
	# if ($scheme = 'http') {
	# 	return 301 https://libacchu.42.fr$request_uri;
	# }

	# location / {
		# try_files $uri /index.php?$args;
		# add_header Last-Modified $date_gmt;
        # add_header Cache-Control 'no-store, no-cache';
        # if_modified_since off;
        # expires off;
        # etag off;
	# }

    location / {
        try_files $uri $uri/ /index.php$args;
    }

	# location ~ \.php$ {
	# 	fastcgi_split_path_info ^(.+\.php)(/.+)$;
	# 	fastcgi_pass wordpress_php:9000;
	# 	fastcgi_index index.php;
	# 	include fastcgi_params;
	# 	fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
	# 	fastcgi_param PATH_INFO $fastcgi_path_info;
	# }
	location ~ [^/]\.php(/|$) { 
		try_files $uri =404;
		fastcgi_split_path_info ^(.+\.php)(/.+)$;
		fastcgi_pass wordpress_php:9000;
		fastcgi_index index.php;
		include fastcgi_params;
		fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
		fastcgi_param PATH_INFO $fastcgi_path_info;
	}
}
